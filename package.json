{
  "name": "safes",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ safes",
    "create-local": "graph create --node http://localhost:8020/ safes",
    "remove-local": "graph remove --node http://localhost:8020/ safes",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 safes",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.55.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "urql": "^4.0.5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
